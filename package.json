{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "gitpush": "git add . && git commit -m 'deploy' && git push origin main",
    "deploy": "pnpm run build && pnpm run gitpush"
  },
  "license": "MIT",
  "devDependencies": {
    "vite": "^4.0.3",
    "vite-plugin-solid": "^2.5.0"
  },
  "dependencies": {
    "solid-js": "^1.6.6"
  }
}
